# AFTERGLOW Drift — handoff for new chat sessions

Этот файл вводит новый чат в курс дела. Прочитай его целиком + файлы, на которые он
ссылается, прежде чем что-то менять. Отвечай пользователю НА РУССКОМ.

## 1. Что это

Браузерная/desktop-игра «AFTERGLOW Drift» — 3D-гонка-дрифт на React + Three.js,
упакованная в нативные macOS-приложения через Electron. Есть несколько карт:
плоский город «Свитлодарск», парковка Costco, трек и новая холмистая карта
«Würzburg» с реальным рельефом из OSM + Mapzen Elevation.

## 2. Как запускать / собирать

```
# корень проекта: <REPO>/game
cd game

# проверка типов
npx tsc --noEmit

# dev-сборка Next
npm run build

# упаковка в нативный .app (macOS ARM)
npx electron-builder --mac --arm64
# результат: game/release/mac-arm64/AFTERGLOW Drift.app
```

Запуск упакованного приложения для теста (нужно для CDP) и CDP-порт к нему:

```
pkill -9 -f AFTERGLOW; sleep 1
"<REPO>/game/release/mac-arm64/AFTERGLOW Drift.app/Contents/MacOS/AFTERGLOW Drift" \
  --remote-debugging-port=9222 > / tmp/app.log 2>&1 &
# приложение раздаёт игру по http://127.0.0.1:41230
curl -s http://127.0.0.1:9222/json/version
```

Подключение к игре через WebSocket CDP для управления авто/скриншотов:
`game/node_modules/ws/wrapper.mjs`.

## 3. Архитектура (ключевые файлы)

- `game/app/race.ts` — игровой цикл, управление (клавиатура W/A/S/D/SPACE,
  Shift=занос, R=перезапуск, E=вход/выход, G=смена стиля, M=карта, Esc=пауза),
  физика, камеры, `reset()`, `groundY()`, HUD. `MapId`/`TOWN_MAPS` — какие карты
  городские (сейчас svitlodarsk + wurzburg). Дебаг-инструменты на `window`:
  `__probe(x,z,top)` — лучевой зонд до поверхности, `__mm(x,z)` — высота пола,
  `__hud` — позиция машины {carX,carZ,carY,x,z,yaw}.
- `game/app/town.ts` — строит город целиком из `TownData`: материалы (DoubleSide),
  земля-меш ровно по сетке высот, дороги/площадки/вода,
  здания (`base`=высота в центроиде, крыша `base+h`), коллайдеры, `height()`.
  Верхние поверхности: вода `-0.08`, трава `+0.015`, площадки/парковки `+0.018`,
  дороги `+0.02`, дорожные диски `+0.025`, земля `0`.
- `game/app/town-data.ts` — тип `TownData` и параметризованный `loadTownData(id)`.
- `game/app/town-details.ts` — пропы (деревья/трава/фонари/бордюры/лавки) с
  параметром высоты.
- `game/app/page.tsx` — реестр карт (Würzburg = id `04`, тег `HILLS`), выбор карты,
  кнопка «drive», карта-мини/фулл.
- `game/app/town-minimax.tsx` — мини- и полная карта, проп `map?`.
- `game/app/visuals.ts` — шейдер неба (закат), зерно-текстура асфальта.
- `game/app/actors.ts` + GLB-модели — авто, BMW S1000RR, персонаж.

## 4. Формат данных карты (`public/maps/<id>.json`)

```ts
type TownData = {
  id, name, spawn: {x,z,yaw}, bounds: [x0,z0,x1,z1],
  buildings: {p:[x,z][], h, kind}[],
  roads: {p:[x,z][], kind, width}[],
  areas: {p:[x,z][], kind}[],
  elev?: {o:[x,z], res, nx, nz, data:number[]}  // сетка высот
}
```

- `svitlodarsk.json` ~380 КБ, **без** `elev` (плоская; везде проверяй `if (data.elev)`).
- `wurzburg.json` ~3.1 МБ: 3800 зданий, 1300 дорог, 2026 площадок;
  bounds `[-3825.4, -3698.8, 3745.5, 3577.8]`; высоты относительные к уровню города
  182.8 м (окрестности `-30..179`). Спавн `{x:-1177.73, z:-542.02, yaw:-3.1117}`.
- Генератор карты: `/var/folders/3m/wnwv2wgs09vdjvzr2m3f9bx80000gp/T/opencode/gen_wurzburg.py`
  (Overpass три зеркала + Mapzen Terrarium z15 PNG). **Скопируй его и cdp-скрипты в
  `<REPO>/game/tools/`, потому что /tmp может очиститься.**

## 5. РЕЛЬЕФ — важно не сломать

Единый источник высоты — `makeHeight(data)` в `town.ts` (линейная интерполяция по
треугольникам, идентична триангуляции плоско-меша земли!). Используй ОДНУ и ту же
функцию для: меша пола, «драпировки» дорог/площадок/воды, расстановки пропов,
`groundY`/физики машины. Если высоты станут расходиться — машина «выезжает
из-под текстур» (это уже чинили, регрессия недопустима).

## 6. Стиль кода

- сочетание `function`/`const` внутри одного тела; плотный код без комментариев
  (НЕ добавляй комментарии).
- `mat(color, roughness)` для материалов; фейсады/крыши/трава/дороги — `DoubleSide`.
- геометрии в батчах через `mergeGeometries(..., false)`; `g.dispose()` после merge.
- `castShadow` только у зданий (фейсады+util); у деревьев/земли — false.

## 7. Как проверять свои изменения (обязательно)

1. `npx tsc --noEmit`
2. `npm run build`
3. `npx electron-builder --mac --arm64`
4. Запусти .app с CDP, выбери карту, поезжай: управление только клавиатурой —
   надо слать `KeyboardEvent` с `code:'KeyW'` (не `key:'w'`).
5. Проверь через `__probe`/`__mm`/`__hud`, скриншот `Page.captureScreenshot` и
   пиксельный анализ (python3 + PIL): сквозных дыр (светлые/небесные пиксели в
   нижней 60% кадра) быть не должно, трава-зелёная не менее нескольких %.

## 8. Текущее состояние и открытые хвосты

Сделано и работает:
- управление клавиатурой без мыши (одобрено), убраны «скользящие» огромные тени,
- карта Würzburg с рельефом, деревья/трава уплотнены (leaves 6500, greens 900/area),
- фикс стыка пол/дорога (линейная высота, накладки; коммит `6c67402`),
- дебаг-инструменты `__probe`/`__mm`/`__hud` в `race.ts`.

Открытые хвосты (может взять тестер/следующий чат):
- часть дорог ведёт «в никуда» / обрывается в склоне — приоритет низкий, но
  заметно (можно подрезать/продолжить дороги по рельефу),
- финальная визуальная проверка картинки после фикса стыка (я сам изображения
  смотреть не могу — оценка только по пиксельным метрикам),
- общие: сообщать пользователю по-русски, коммит только своих файлов.

## 9. Правила параллельной работы (несколько чатов)

- каждый чат: ОДНА функция/правка, фокус, не трогай чужие файлы;
- коммит маленькими порциями с явными именами (`backup-N`, `feature-<name>`);
- тестер-чат: прогоняет tsc/build/package, CDP-гонку и пиксельную проверку,
  чинит баги и коммитит `fix-...`;
- перед финалом один чат делает интеграционный прогон всех фаз.