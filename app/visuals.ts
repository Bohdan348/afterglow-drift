import * as T from 'three';
import {EffectComposer} from 'three/addons/postprocessing/EffectComposer.js';
import {RenderPass} from 'three/addons/postprocessing/RenderPass.js';
import {UnrealBloomPass} from 'three/addons/postprocessing/UnrealBloomPass.js';
import {OutputPass} from 'three/addons/postprocessing/OutputPass.js';
export function createEffects(renderer:T.WebGLRenderer,scene:T.Scene,camera:T.Camera,scale=1){const sw=Math.max(1,Math.round(innerWidth*scale)),sh=Math.max(1,Math.round(innerHeight*scale));const target=new T.WebGLRenderTarget(sw,sh,{type:T.UnsignedByteType,samples:scale<1?0:2});const composer=new EffectComposer(renderer,target);composer.addPass(new RenderPass(scene,camera));const bloom=new UnrealBloomPass(new T.Vector2(sw,sh),scale<1?.15:.22,.45,1.3);composer.addPass(bloom);composer.addPass(new OutputPass());return{render(){composer.render()},resize(){const w=Math.max(1,Math.round(innerWidth*scale)),h=Math.max(1,Math.round(innerHeight*scale));target.setSize(w,h);composer.setSize(w,h)},dispose(){bloom.dispose();composer.dispose();target.dispose()}}}
export function grainTexture(){const canvas=document.createElement('canvas');canvas.width=canvas.height=128;const ctx=canvas.getContext('2d')!,image=ctx.createImageData(128,128);let seed=22;for(let i=0;i<image.data.length;i+=4){seed=seed*16807%2147483647;const n=135+seed%85;image.data[i]=image.data[i+1]=image.data[i+2]=n;image.data[i+3]=255}ctx.putImageData(image,0,0);const texture=new T.CanvasTexture(canvas);texture.wrapS=texture.wrapT=T.RepeatWrapping;texture.repeat.set(12,12);return texture}
