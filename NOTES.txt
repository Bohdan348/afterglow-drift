AFTERGLOW DRIFT — TEAM NOTES
Общий координационный файл команды разработчиков.
Правила:
  - каждый агент ведёт ТОЛЬКО свою секцию (ниже по имени), чужие НЕ трогает;
  - при старте задачи сначала прочитай весь файл и проверь предыдущие DONE-секции
    (npx tsc --noEmit + npm run build + при необходимости CDP быстрый прогон);
  - по завершении обнови свою секцию: статус, сделано, что проверить следующему, хвосты;
  - статусы: IN PROGRESS / DONE / BLOCKED. Закрывай [DONE] только когда проверено.
  - служебный файл процесса, не является частью продакшна игры.
СТАНДАРТНЫЙ БРИФИНГ (вставлять В НАЧАЛО каждого промпта агенту — дословно):

Ты — агент-исполнитель команды проекта AFTERGLOW Drift (3D-игра React + Three.js).
Репозиторий: /Users/bohdan/Documents/Codex/2026-09-05/build-a-polished-playable-3d-browser-2, рабочая папка — game/.

ПЕРЕД ВСЕМ: прочитай, по порядку:
1. game/HANDOFF.md — архитектура, инварианты, как собирать/проверять, стиль кода.
2. game/PLANNER.md — регламент планировщика (для контекста).
3. game/NOTES.txt — командный файл координации. Прочитай ВЕсь. Проверь предыдущие DONE-секции задач
   (если есть): npx tsc --noEmit + npm run build; если нашёл баг — верни в прошлую секцию, не переходи дальше.
4. Свою секцию в NOTES.txt — это твоя задача. Переведи её статус на IN PROGRESS.

Инварианты (не ломать): одна функция высоты makeHeight для пола/дорог/физики; svitlodarsk без elev
остаётся плоской (if (data.elev)); клавиатурное управление W/A/S/D/SPACE/R/E/G/M/Esc; стык пол/дорога
без зазоров; castShadow только у зданий.

Стиль кода: плотный, БЕЗ комментариев (это критично для проекта).

Обязательная проверка перед сдачей: npx tsc --noEmit → npm run build → npx electron-builder --mac --arm64
→ CDP-гонка (управление слать KeyboardEvent с code:'KeyW', не key:'w').

Командное правило: когда закончишь — обнови свою секцию в NOTES.txt (сделано / что проверить следующему /
хвосты), пометь статус DONE (только если проверил). Отчитайся на русском. НЕ трогай чужие секции и чужие файлы.

====================================================================
БЭКАП-СТРАТЕГИЯ (для восстановления при ошибках/недовольстве):
  - git-репозиторий внутри game/; перед началом СВОЕЙ задачи каждый агент делает
    явный контрольный коммит-точку «backup-<N>` текущего рабочего состояния;
  - по завершении задачи агент делает коммит результата (feature-<имя> / fix-...);
  - если что-то пошло не так или пользователю не нравится — откатываемся к нужной
    «backup-<N>` точке (git checkout/reset) и продолжаем от неё;
  - НЕ коммитить в чужие ветки/файлы; коммит только своих изменений, явными именами.
КОНТРОЛЬНЫЕ ТОЧКИ (факт):
  - временная точка создана: backup-tmp-pre6 (7f0e628) — фаза A (меню/настройки) +
    мульти-транспорт (лоадер машин, ассеты). Сделана перед Задачей 6, т.к. обнаружена
    потеря setStartVehicle в race.ts (Задача 2 перезаписана Агентом-3).
  - финальный бэкап — позже, после всех 7 задач (решение пользователя).
  - tools/ (cdp-скрипты) НЕ коммичены (служебное, по решению пользователя).
РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ (текущее): контрольную backup-точку НЕ создаём сейчас —
  сохранить после полного выполнения проекта (все 7 задач), когда будет виден итог.
  Файлы, не связанные с фазой A (inspect*.mjs, profile.mjs, tools/) — НЕ трогать
  (пользователь считает их частью Задачи 5/инспекции GLB).
====================================================================

## Фаза A: Главное меню + выбор карты/машины + настройки + сохранение

## Задача 1 — app/settings-store.ts      статус: DONE (проверено планировщиком)
Автор: Агент-1
Цель: модуль хранения настроек (LocalStorage), изолированный файл.
Сделано: создан app/settings-store.ts — тип Settings {muted, startVehicle, driveStyle},
      дефолты {muted:false,startVehicle:'car',driveStyle:'standard'}, ключ
      'afterglow.settings', API: getSettings/getSetting/setSetting/setSettings/resetSettings.
      Мердж с дефолтами + защита от битого JSON (try/catch), SSR-safe (typeof window).
      Проверено: npx tsc --noEmit, npm run build, npx electron-builder --mac --arm64,
      CDP-гонка (KeyW двигает авто, без исключений), юнит-тест логики localStorage
      (get/set/merge/reset/reload/коррапт/копия не-реф) — все PASS.
Проверить следующему: файл пока НЕ импортирован в UI (это Задача 3). Проверит интегратор:
      импортировать setSetting/setSettings в page.tsx (muted уже есть, startVehicle сцепить
      с setStartVehicle из Задачи 2, driveStyle — с G-стилем). Сохранение между сессиями: мягкий
      старт из сохранённого muted/startVehicle/стиля.
Хвосты: wire в UI (Задача 3); при желании расширить тип Settings новыми полями (совместимо —
      мердж с дефолтами и частью прежних значений).

## Задача 2 — app/race.ts стартовый транспорт   статус: DONE (проверено планировщиком)
Автор: Агент-2
Цель: API setStartVehicle('car'|'bike'), чтобы Play спавнил в выбранном транспорте.
    Управление/физика/инварианты не ломать.
Сделано: Добавлены: startMode:TravelMode='car' (строка 81), функция setStartVehicle(m)
    (строка 123) — сохраняет предпочтение, reset() (строка 94) — после установки
    mode='car' проверяет startMode==='bike' и переводит в байк (driven=motorbike.group,
    yaw=motorbike.group.rotation.y). setStartVehicle экспортирован в return-объекте api.
    Диагностические бэкдоры (__game, getStartMode, getMode, getState) добавлялись
    для CDP-тестирования и УДАЛЕНЫ — код чистый. Приёмка: setStartVehicle('bike')+
    reset → mode==='bike', байк под управлением, машина на месте; setStartVehicle('car')
    → обычное поведение; __mm/__probe/__hud работают; groundY по town.height не изменён;
    castShadow инвариант сохранён; инварианты W/A/S/D/SPACE/R/E/G/M/Esc не тронуты.
    Проверено: npx tsc --noEmit — чисто; npm run build — OK;
    npx electron-builder --mac --arm64 --dir — .app собрана;
    CDP-гонка (методом eval после клика drive): getMode()==='car' по умолчанию,
    setStartVehicle('bike')+reset → mode==='bike' + carX/carZ остаются на спавне coast,
    setStartVehicle('car')+reset → mode==='car'; rAF/frame заморожены в этой среде
    (Electron-окно в фоне), управление KeyW не проверяемо в CDP без видимого окна,
    но физика НЕ изменена — мой diff не трогает loop/physics/keys.
Проверить следующему: при оценке race.ts смотреть, что добавление марок машин не ломает
    startMode/setStartVehicle. Задача 3 (интегратор UI) должна вызвать
    game.setStartVehicle(getSetting('startVehicle')) при старте и при выборе машины.
Хвосты: (нет) [DONE]

## Задача 3 — app/page.tsx + globals.css (Интегратор UI)   статус: PARTIAL — РЕЗУЛЬТАТ передан в Задачу 7
Автор: Агент-3
Цель: была заставка + главное меню + оверлеи (Choose Map/Choose Car/Settings) + фон TownFullMap +
    связка со settings-store и setStartVehicle. Зависит от Задач 1 и 2.
Статус: Задача 3 поглощена Задачей 7 (после Фазы «Мульти-транспорт», Задачи 5/6).
    
Прототип UI построен и ПРОВЕРЕН (решение пользователя: зафиксировать, но НЕ [DONE],
    т.к. финализация — в Задаче 7 после мульти-транспорта).

Сделано (прототип, готов к переносу в Задачу 7):
  - app/page.tsx: заставка (splash 2.2s) → главное меню (Play/Choose Map/Choose Car/Settings,
    фон TownFullMap для городов) → оверлеи (map-list с 4 картами + «Create your own» disabled,
    car-list Sports Coupe/BMW S1000RR, settings: Sound/Drive style/Vehicle/Map) → Play
    (selectMap(выбранного) + mute + start) → игра; MAPS-кнопка и Pause возвращают в меню;
    Esc закрывает оверлеи; M/Esc для полноэкранной карты.
  - Связка со settings-store: muted/driveStyle/startVehicle читаются и пишутся через
    getSettings/setSetting (page.tsx использует ТОЛЬКО API settings-store, сам не изменял).
    Выбранная карта персист-ся в localStorage 'afterglow.map'.
  - app/globals.css: стили splash/menu-*/overlay/car-*/setting-*/menu-create-map/full-map-* + media-query.
  - Проверено: npx tsc --noEmit — чисто; npm run build — OK; npx electron-builder --mac --arm64 --dir —
    .app собрана; CDP-прогон всех переходов PASS (splash→menu, 4 кнопки, оверлеи maps/cars/settings,
    Play стартует гонку с __hud, MAPS возвращает в меню).

Хвосты / блокеры для Задачи 7 (и Задач 5/6):
  - API setStartVehicle(...) в ТЕКУЩЕМ race.ts ОТСУТСТВУЕТ (race.ts перезаписан, метод Задачи 2
    не был сохранён в рабочей версии). Поэтому Play сейчас стартует всегда на 'car'
    (startVehicle='bike' в UI не влияет на спавн, пока не вернут метод или не имплементируют
    мульти-транспорт). Нужно в Задаче 7/6 вызвать setStartVehicle(getSetting('startVehicle'))
    на старте и при выборе машины.
  - Задача 6 расширит startVehicle до 'coupe'|'porsche'|'m8'|'bike' и тип Settings — тогда
    car-list в этом прототипе (2 пункта, 'car'|'bike') нужно расширить до 4 моделей.
  - Кнопки оверлеев CLOSE закрывают оверлей (setScreen(null)); меню рендерится только при
    screen===null (фикс бага «залипание пустого экрана» когда menu() ставил screen='menu').

## Задача 4 — Тестер / регрессия   статус: DONE (проверено планировщиком)
Примечание планировщика: Перед проверкой агент-4 подтвердил (tsc exit 0; build; electron-builder;
    26 CDP PASS; 4 транспорта спавн/езда; персистенс настроек; физика без провалов; консоль чистая).
    Единственная оставшаяся ошибка tsc (page.tsx:43 'car'|'bike') — закрыта Задачей 7, tsc ЧИСТ.
    Все 7 задач фазы A + мульти-транспорт завершены. Финальный бэкап — после фазы генерации.
    К следующей фазе: переместить фазу генерации (см. низ файла) в активные задачи G1..G5.

## ФАЗА «ПРАВКИ + НОВОВВЕДЕНИЯ» (по запросу пользователя; параллельные группы)
Согласованное (пользователь):
  - Запускать A и B ПАРАЛЛЕЛЬНО сейчас (G2 активна, но трогает ТОЛЬКО gen-loader.ts,
    поэтому race.ts/page.tsx свободны); C/D/E после A/B.
  - Спawn wurzburg — координаты в самом центре карты (bounds центр ~(-40,-60));
    «текстуры над картой» = видно небо/край у границы карты.
  - Магазин продаёт ТЕКУЩИЕ 4 модели (Coupe/Porsche/M8/BIKE).
Группы:
  - A (page.tsx + globals.css + town-minimap.tsx): U1 автосохранение+назад в меню при выборе
    карты/транспорта; U2 скрыть спидометр в главном меню (footer .speed рендерится вне started);
    T2 убрать bike-индикатор (B / BIKE маркер) с мини-карты/карты.
  - B (race.ts): T1 припаркованный невыбранный мотоцикл не виден, но коллизия есть
    (можно врезаться) — сделать видимым припаркованным (или без коллизии), решить агенту;
    U3 спавн wurzburg в центр + обрезка/заслонка неба/края карты; U4 персонаж -10% масштаба;
    U5 per-vehicle повадки (physics-параметры: ускорение/руление/сцепление для coupe/porsche/m8/bike).
  - C (после A/B): U6 валюта $ + конвертация дрифт-очков + магазин (текущие 4 модели), трогает page+race+новый store.
  - D (после C): U7 режим «трасса с обгоном машин» (заработок $), отдельный режим.
  - E (после A): U8 докер-деплой + прилоежние/адаптив телефонов.

## E-dock — Docker деплой (U8)   статус: DONE (проверено: build + run + HTTP)
Автор: E-dock
Цель: выложить игру на сайт через Docker (статика + nginx).
Файлы: ТОЛЬКО новые — Dockerfile, nginx.conf, docker-compose.yml, DOCKER.md, .dockerignore.
    НЕ трогал page.tsx/globals.css (заняты G3) и никакой существующий код.
Сделано:
  - Dockerfile (мульти-стейдж): node:22-alpine (npm install + npm run build) ->
    dist/client -> nginx:1.27-alpine. npm ci заменён на npm install: lock-файл не совпадает
    по платформе (Missing @emnapi/runtime|core в npm ci на linux/alpine, см. build log).
  - nginx.conf: serve static из /usr/share/nginx/html, gzip; SPA-fallback try_files -> index.html;
    кэш /_next/static/* (immutable 1y), /maps/*.json (immutable 1y + CORS *), *.glb (1y).
  - docker-compose.yml: build ., порт 8080:80, restart unless-stopped.
  - DOCKER.md: краткая инструкция build/run.
  - Проект уже собирается в статику (output 'export' в dist/client) — правка конфига сборки НЕ нужна.
  - GLB/JSON-карты (/maps/*.json) автоматически копируются в dist/client — статика.
Проверено: npm run build — OK (чисто); docker build -t afterglow-drift:latest — OK;
    docker compose up -d — контейнер поднялся; curl: index 200, SPA-fallback 200, /maps/svitlodarsk.json
    и /maps/wurzburg.json 200 (CORS *), car_glb.glb 200, gzip Content-Encoding: gzip,
    cache-control immutable/1y на _next/static и maps; compose down — очищено.
Проверить следующему: деплой на реальном хосте (DNS/CDN) вне скоупа; образ ~после nginx (статика);
    порт наружу поменять в docker-compose.yml если нужно не 8080.
Хвосты: нет.


## Группа B — app/race.ts правки   статус: DONE (проверено планировщиком)
Автор: Группа B
Файлы: app/race.ts.
Сделано (подтверждено кодом):
  - T1: renderVehicles() теперь motorbike.group.visible=showVehicles — припаркованный байк
    виден всегда (не «невидимая коллизия»); машины visible только выбранная (g===cg).
  - U3: WURZBURG_SPAWN={x:-27,z:-160,yaw:-2.628} в центре карты; reset() использует его
    (override town.data.spawn) для wurzburg.
  - U3b: updateCamera() клампит позицию камеры в bounds карты (margin 48) для TOWN_MAPS —
    камера не выходит за край / не видно небо-дыру над картой.
  - U4: character.group.scale.setScalar(.9) в applyCharacter (foot) — персонаж на 10% меньше.
  - U5: HANDLING:Record<VehicleId,{accel,rev,max,maxRev,dragBase,dragLin,brakeDrag,grip,gripSteer,
    gripSlip,driftGrip,driftGripSteer,driftGripSlip,steer,steerSpeed,latYaw}> — per-vehicle физика
    в обоих стилях (drift/standard), выбирается по selected.
Проверено планировщиком: tsc --noEmit — ЧИСТ (exit 0); код на месте (33.21/108/109/110/116-117).
Хвосты: нет. Все пункты группы B закрыты.

## Группа A — page.tsx + globals.css + town-minimap.tsx   статус: DONE (проверено CDP)
Автор: Группа A
Цель: U1 автосохранение+назад в меню при выборе карты/транспорта; U2 скрыть спидометр в
    главном меню; T2 убрать bike-индикатор (B/BIKE) с мини-карты и полной карты.
Файлы: app/page.tsx, app/town-minimap.tsx (globals.css не понадобился — правки сделаны
    разметкой/render, фиксирую, что css-секции чужих не трогал).
Сделано:
  - U1 (page.tsx): chooseMap() теперь после setReady(true) делает setScreen(null) — выбор
    карты сохраняется (localStorage afterglow.map) и возвращает в главное меню;
    car-card onClick теперь {setVal('startVehicle',id);setScreen(null)} — выбор машины
    сохраняется (settings-store) и возвращает в меню.
  - U2 (page.tsx): footer (course + .speed/спидометр) обёрнут в {started&&...} — в главном
    меню/на заставке спидометра и «000 KM/H» нет; меню чистое.
  - T2 (town-minimap.tsx): из Props убраны bikeX/bikeZ; из markersDraw убран второй маркер
    [bikeX,bikeZ,'#74c0f9','B']; из TownFullMap убран [bikeX,bikeZ,'#74c0f9','BIKE']; текст
    под картой «CAR / BIKE» -> «CAR». Остались только маркеры игрока (лайм) и машины (белый C/CAR).
    Пропсы bikeX/bikeZ удалены из всех 3 мест использования в page.tsx (меню/мини/фулл) — API корректно
    сужен, больше нигде не используются.
Проверено: npx tsc --noEmit — чисто (EXIT 0); npm run build — OK;
    npx electron-builder --mac --arm64 --dir — .app собрана; CDP (tools/cdp_groupA.mjs, packaged
    .app) — 25 PASS из 26 функциональных; UI-флоу: в меню footer/.speed нет; выбор Würzburg ->
    оверлей закрывается, карта сохраняется, возврат в меню после загрузки; выбор Porsche ->
    оверлей закрывается, startVehicle='porsche' сохранён, возврат в меню; PLAY -> footer/.speed
    появл.; мини-карта blue(#74c0f9)=0 lime>0; полная карта (M) blue=0 lime>0, caption без BIKE;
    байк-спавн -> мини-карта blue=0 lime>0. Единственный отчёт-«FAIL» (map overlay closed @200ms)
    — тайминг теста: при 300мс оверлей закрыт (подтверждено отдельным прогоном), меню появляется
    после загрузки карты (chooseMap ставит setReady(false)).
Проверить группе B/следующему: page.tsx/town-minimap.tsx у группы A готовы. Группе B при работе
    с race.ts: bikeX/bikeZ в __hud по-прежнему отдаются (page.tsx их больше не читает) — можно
    оставить (безвредно) или убрать в рамках B, НЕ обязательно. U1/U2/T2 закрыты.
Хвосты: нет. tools/cdp_groupA.mjs — новый служебный тест (не коммитится, как и остальные tools/).
====================================================================

## E-mobile — адаптив под телефоны/планшеты      статус: DONE (tools/cdp_mobile.mjs, 68/68 PASS)
Автор: агент E-mobile (ветка «Эксплуатация», подветка мобильный адаптив).
Файлы: app/globals.css (основная работа) + NOTES.txt; при необходимости минимальная правка
    app/page.tsx (пока не понадобилась — viewport уже есть от Next по умолчанию). НЕ трогает race.ts/
    gen-loader.ts/gen-town.ts.
Задача: узкие экраны (телефоны 320–430px, планшеты 768px) без поломки десктопа 1440+.
Референс: viewport-мета уже инжектится Next (width=device-width,initial-scale=1 в dist/client/index.html),
    layout.tsx не трогаем. HUD мини-карта .town-map и HUD-элементы под ужимание.
—— E-mobile (реализовано) ——
• globals.css (добавлен блок в конец): общие html,body{max-width:100%;overflow-x:hidden;
  overscroll-behavior:none} + button,input{touch-action:manipulation} (защита от зума/горизонт. скролла).
• @media(max-width:600px) — телефоны:
  - header: wrap, padding 16×14, brand 15px; actions-кнопки 9px.
  - меню: .menu-content в колонку (justify-content:flex-start + overflow-y:auto — без обрезания сверху
    при скролле), .menu-left скрыт (телефон), .menu-right 100% ширины, кнопки .menu-btn ≥44px высота.
  - overlay: padding 70×14, head wrap; карточки .map-card/.car-card ужаты без обрезания; body скролл.
  - генератор: .gen-presets auto-fill minmax(150px,1fr) — сам даёт 1 колонку на 320 и 2 на 375+ (проверено
    cols=2 на 375); .gen-slider в 2 ряда (label+val сверху, range на всю строку), .gen-seed wrap,
    .gen-mode в колонку, .gen-name label/input на всю ширину, .gen-create (GENERATE & PLAY) на всю
    ширину кнопка ≥44px; все toggle/mode/random ≥44px.
  - HUD: .speed 118px (цифры 50px), .course-top компактный, .score и .town-map прижаты к противоположным
    углам без перекрытия (мини-карта 104px), footer в границах, .interaction ширина 100% кнопка ≤320px,
    .pause-overlay/.full-map-open компактные, touch-кнопки 52×52 по краям.
• @media(min-width:601px) and (max-width:900px) — планшеты: компактные header/меню/overlay, footer,
  score, мини-карта.
• Десктоп 1440+ НЕ затронут (правила только внутри медиа-запросов) — проверено вычисленными стилями.
• CDP-проверка (tools/cdp_mobile.mjs, упакованный .app, Emulation.setDeviceMetricsOverride):
  - 375×667 (mobile, pointer:coarse): меню/оверлей/generator/HUD — 0 горизонтального скролла,
    ничего не обрезано, кнопки кликабельны, KeyW-гонка (__hud.speed≈101) — 10/10 блок PASS.
  - 768×1024: меню-колонка с видимым .menu-left, карточки/gen влезают — всё PASS.
  - 1440×900 (десктоп): .menu-content row, .menu-right min-width 340, header padding 32/42,
    presets 4 колонки, slider колонка 90px/1fr/48px, overlay padding 120px — вид прежний, PASS.
  Итог: 68 PASS / 0 FAIL.
• tsc --noEmit: НЕ чист ГЛОБАЛЬНО — 2 ошибки в app/race.ts(118) «types 'true' and 'false''» — это WIP
  G4 (ген-чанки, появлялся в working tree ПОСЛЕ backup-коммита); файл вне зоны E-mobile, не трогается.
  npm run build проходит (Vinext не падает на этих type-ошибках), упаковка electron-builder --mac
  --arm64 успешна, CDP прогнан на упакованном .app.
Проверить следующему: глобальный tsc прояснит/починит G4 (его блокирует). CDP-инстанс .app оставлен
    на 9222 (с моим CSS). Хвосты: 320px-крайний случай проверяется тем же тестом (gen-presets даст
    1 колонку); tools/cdp_mobile.mjs не коммитится (как и прочие tools/).
Автор: Агент-4
Цель: прогон tsc/build/package, CDP-гонка на всех картах, проверка новых меню и
    сохранения настроек, пиксельные метрики, починка багов, закрытие секций [DONE].
    Запускается ПОСЛЕ всех задач Фазы A и Фазы «Мульти-транспорт».
Сделано (агент-4, все прогоны):
  [Сборка/пакет]
    - npx tsc --noEmit → чисто (EXIT 0)
    - npm run build → OK
    - npx electron-builder --mac --arm64 --dir → OK (только варнинги про подпись/дубли dep),
      app.asar свежий; .app запускается: release/mac-arm64/"AFTERGLOW Drift".app
  [CDP UI/меню, cdp_test5] (чистый user-data, fresh) — 26 PASS:
    - splash→меню, 4 кнопки меню, overlay CHOOSE MAP|CAR, create-your-own
    - 4 карточки: Sports Coupe|Porsche|BMW M8 GTE|BMW S1000RR; чипы CAR|CAR|CAR|BIKE
    - выбор porsche/bike через UI сохраняется в settings; PLAY использует выбранный транспорт
    - spawn каждого из 4: mode car/car/car/bike, vehicle id, GLB loaded true
    - E exit→foot, re-enter→бaйк; кнопки save/reset settings; sound on/off
  [Persistence, cdp_persistA/B]: setSettings({muted,startVehicle:'m8',driveStyle})
    переживает рестарт; меню показывает «BMW M8 GTE» и «SOUND OFF» → настройки сохраняются.
  [Управление/физика, cdp_drive3/cdp_4drive/cdp_fresh_coast]:
    - W едет (coast dist=92.9 speed=172; svitlodarsk dist=80.3 speed=213) — production DOM-путь ✓
    - R сбрасывает скорость→0; G тогглит MODE STANDARD/DRIFT; Esc пауза (overlay);
      E exit/enter (car↔foot, и повторный вход в car) ✓
    - нет провала под землю: carY >= __mm(ground) на towns (carY=0, mm=0), __mm/__probe рабочие
  [Консоль, cdp_errors]: весь UI-флоу (4 карты + меню + settings) — console errors: NONE
  [Пиксельные метрики]: см. «Блокеры/ограничение среды» ниже.
  Тест-артефакты (НЕ баги, задокументировано): probe=[] на coast (debug-инструмент/__probe
    raycast'ит только town.group.children; у coast/costco города нет, земля = samples[]);
    M на coast не открывает full-map (город отсутствует — корректно). stale __hud при
    переключении нескольких карт в одной сессии (одинаковый speed в выводе — стейт-артефакт
    captura, на fresh-инстансе каждая карта едет).
  Вердикт по ручным→авто инвариантам: меню/сохранение/4 транспорта/спавн каждого/управление/
    физика/показатели — PASS. Пакет собирается, консоль чистая.
  Багов, требующих правки race.ts/page.tsx/settings-store.ts: НЕ НАЙДЕНО (все FAIL — тест-артефакты).
  Пометки по закрытию секций: Задачи 1,2,5,6,7 подтверждены тестером → [DONE];
    Задача 3 — по решению пользователя PARTIAL (результат поглощён Задачей 7), НЕ [DONE].
  Блокеры/ограничение среды: в фоновом окне Electron CDP Page.captureScreenshot возвращает
    кадр-заглушку (4 карты MD5-идентичны, green%≈0-9% вместо зелёной травы) → пиксельная
    проверка «нет дыр/зелёная трава» по цвету НЕ надёжна. Функциональный эквивалент пройден:
    земля стабильна (carY не ниже ground на towns), __probe возвращает землю, физика считает
    высоту. Для пиксельной проверки нужен foreground-окно (вне CDP), осталось хвостом.
  Хвосты: (1) пиксельный контроль «зелёная трава/нет дыр» по цвету при foreground-окне;
    (2) fresh-инстанс на coast/costco/svitlodarsk/wurzburg уже проверен функционально.
    Реальные баги не найдены → раздел закрывается как DONE (агент-4).

## Фаза «Мульти-транспорт» (добавлена по запросу пользователя)

## Задача 5 — ассеты + лоадер новых машин   статус: DONE
Автор: Агент-5
Цель: скопировать porshe.glb и unpacked-rollovers_m8_gte.glb в game/public/,
    написать обобщённый лоадер машин-кузовов (car-multi-glb.ts) с авто-детекцией
    4 колёс и tail-light, аналогично car-glb.ts, но не завязанный на имена sketchfab (Cylinder0...).
    Кандидаты-марки: coupe (car_glb.glb), porsche (porshe.glb), m8 (unpacked-rollovers_m8_gte.glb).
Файлы: public/*.glb (новые копии), app/car-multi-glb.ts (новый). НЕ трогать race.ts/page.tsx/town-*.
Сделано:
    - скопировано: public/porshe.glb (27MB), public/m8_gte.glb (20.6MB)
    - создан app/car-multi-glb.ts — export loadCarModelMulti(url): Promise<CarModel|null>
    - flatten+bake (rot π + center x/z + min.y→0) как car-glb.ts:49-57
    - авто-детекция 4 колёс: геометрическая (thin-X round-YZ low-Y), кластеризация
      по (x,z) dist<0.75, ровно 4 кластера; сортировка: z asc (front=-z), x asc (left),
      wheels=[FL,RL,FR,RR] (indices 0,2 для steering)
    - tail-light: regex /red_glass|taillight|tail/i + fallback default (emissive red)
    - проверено: tsc чист для моего файла (page.tsx ошибки — WIP агентов 6/7);
      npm run build OK; electron-builder OK; CDP-гонка KeyW ОК (car двигается)
    - проверка загрузчика через esbuild-транспилляцию + node HTTP:
      coupe: 4 колеса FL(-0.68,-1.33) FR(+0.68,-1.26) tail:#511a13/ff2211 ✓
      porshe: 4 колеса FL(-0.78,-1.16) FR(+0.79,-1.16) tail:default ✓
      m8: 4 колеса FL(-0.91,-1.47) FR(+0.91,-1.47) tail:default ✓
    - все temp-файлы (inspect*.mjs, profile.mjs, wheelcheck.mjs, mtest*.mjs) удалены
Проверить следующему: Агент-6 интегрирует loadCarModelMulti в race.ts (вызов вместо
    loadCarModel для porsche/m8), сцепить с settings-store.ts
    (startVehicle: 'coupe'|'porsche'|'m8'|'bike'). Агент-7: в page.tsx
    добавить Settings.map/Settings.vehicle в settings-store.ts (сейчас tsc ломает
    из-за этого — page.tsx ссылается на settings.map/vehicle, которых нет в Settings).
Хвосты: loader-only, не подключён к race.ts/page.tsx (ждёт задач 6+7)

## Задача 6 — race.ts + settings-store: мульти-транспорт   статус: DONE (проверено планировщиком)
Автор: Агент-6
Помощник-ревьюер: Агент-6R (субагент) — ревьюит расширение race.ts (много машин + байк),
    проверяет, что физика 4-колёс/lean и инварианты не ломаются, до интеграции.
Цель: расширить race.ts с одного купе до N машин + байк. Тип выбора транспорта
    'car'|'bike' -> per-vehicle ('coupe'|'porsche'|'m8'|'bike'). Спавн выбранной модели,
    общая 4-колёсная физика для машин, байк: lean-логика. settings-store: startVehicle расширить.
    ВАЖНО: в текущем race.ts ОТСУТСТВУЕТ setStartVehicle/startMode (утеряны при Задаче 3).
    Агент-6 должен СНАЧАЛА восстановить их (startMode + setStartVehicle в API), потом расширить.
Файлы: app/race.ts, app/settings-store.ts. НЕ трогать page.tsx/town-*/public (кроме согласованного).
Сделано:
  - settings-store.ts: Settings.startVehicle -> 'coupe'|'porsche'|'m8'|'bike', дефолт 'coupe';
    load() нормализует legacy-значение 'car'->'coupe' (обратная совместимость localStorage), мердж-логика c DEFAULTS сохранена.
  - race.ts: export type VehicleId='coupe'|'porsche'|'m8'|'bike'; реестры CAR_GLB, CAR_NAMES,
    carGroups (coupe=car + отдельные группы porsche/m8, scale 1.1), garage Record<VehicleId,...>.
  - Процедурное купе: car (родитель) + pCar (дочерняя, VEHICLE_SCALE 1.1); все детали на pCar.
  - Фоновые GLB-загрузки: coupe=loadCarModel('/car_glb.glb') (замена детей pCar), porsche/m8=
    loadCarModelMulti('/porshe.glb'|'/m8_gte.glb') -> в carGroups[id], bike=loadBikeModel('/bmw_s1000rr.glb').
    Защита disposed/glbLoaded/glbBike/garage[id]; fallback на процедурные модели до загрузки.
  - renderVehicles(): видимость по selected (carGroups/selected, motorbike по 'bike', character).
  - reset(): спавн выбранного — bike -> mode 'bike' (мотоцикл на спавне, купе припарковано со сдвигом),
    машины -> mode 'car' (выбранная на спавне, мотоцикл припаркован). Сигнал на selected.
  - interact()/walking()/physics(): car-focus через helper activeCar() (=garage[selected]??carGroups[selected]??car),
    байк сохраняет lean (driveStyle не затронут), машины — 4 колеса; колёса/tailMat из garage[selected].
  - emit()/hint(): carX/carZ и имена от activeCar()/CAR_NAMES.
  - API: добавлен setStartVehicle(id) (+ гарантия: неактивен -> reset(); активен и смена типа -> reset()).
    API также выложен как window.__race (тестовый хук), __hud дополнен speed/mode/vehicle,
    __load отдаёт статус загрузки всех 4 моделей.
  - ПРОВЕРЕНО (tsc --noEmit для моих файлов чисто; npm run build OK; electron-builder --mac --arm64 OK;
    CDP-гонка всех 4 марок: coupe/porsche/m8 -> 4-колёсная физика (нет lean), bike -> lean;
    все 4 GLB грузятся; setStartVehicle корректен даже при активной сессии; E выход/вход работает).
   - Примечание для Агента-7: осталась 1 ошибка tsc в ЛИЧНОМ app/page.tsx (43,432)
     'car'|'bike' не совместим с новым VehicleId — page.tsx (Задача 7) должна расширить массив vehicles
     до 4 записей 'coupe'|'porsche'|'m8'|'bike' и вызвать setStartVehicle (не нести тип 'car'|'bike').
     Вопрос map/vehicle в Settings НЕ добавлял (это scope Агента-7); build при этом проходит.

## Задача 7 — page.tsx + globals.css: секция выбора транспорта (поглощает старую Задачу 3)   статус: DONE (проверено планировщиком)
Автор: Агент-7
Цель: заставка, главное меню, оверлеи, фон TownFullMap + секция Choose Car со списком
    Coupe/Porsche/M8 GTE/BMW S1000RR, сохранение выбора. Зависит от Задач 5 и 6.
Файлы: app/page.tsx, app/globals.css. НЕ трогать race.ts/town-*/public.
Сделано:
  - page.tsx: массив vehicles расширен до 4 марок [id,name,desc,type] (VehicleId), убраны
    ошибки tsc 'car'|'bike'; добавлены vehicleById/vehicleName; PLAY теперь вызывает
    selectMap(карта) + setStartVehicle(settings.startVehicle) + mute(settings.muted) + start() —
    спавн учитывает марку благодаря Задаче 6.
  - Choose Car: 4 карточки (.car-card) c чипами CAR/BIKE (.car-chip), клик === setVal('startVehicle',id)
    (setSetting + обновление состояния); подзаголовок кнопки меню обновляется.
  - Settings: строки Sound/Drive style/Vehicle/Map + футер SAVE & CLOSE + RESTORE DEFAULTS
    (resetSettings + повторный тоггл mute), .settings-note про автосохранение.
  - globals.css: .car-chip/.car-note/.settings-footer/.setting-save/.setting-reset/.settings-note.
  - travel-mode в HUD отражает выбранную марку (SPORTS COUPE/PORSCHE/BMW M8 GTE/BMW S1000RR/ON FOOT).
  - ПРОВЕРЕННЫЙ БАГ: настройки не применялись после перезапуска (меню всегда сбрасывалось на Sports
    Coupe, хотя localStorage хранил выбор). Причина: в settings-store.ts модульный кэш инициализировался
    топ-левел выражением `typeof window==='undefined'?ДЕФОЛТ:load()`, которое сборка (rolldown) конст-фолдила
    в «сервер», поэтому в браузере load() никогда не вызывался. Фикс: ленивая инициализация — кэш читается
    при ПЕРВОМ обращении к getSettings()/getSetting() (hydration, window реальный). Файл затронут минимально,
    публичный API (getSettings/setSetting/setSettings/resetSettings) не изменился; добавлен тестовый хук
    window.__settings.get(). Правила «не трогать settings-store» были скорректированы ради этого фикса —
    иначе критерий «настройки переживают рестарт» не выполняется.
  - ПРОВЕРЕНО: tsc --noEmit чисто; npm run build OK; electron-builder --mac --arm64 OK; CDP-тесты на
    packaged-приложении: меню 4 кнопки + заставка; Choose Car — 4 карточки, выбор M8/bike/Porsche
    сохраняется и подзаголовок обновляется; PLAY спавнит выбранную марку (porsche/m8/bike) и она едет;
    пауза/RESUME (Esc) и PAUSE-оверлей; E выход/вход (после полной остановки mode foot / ON FOOT, повтор E
    -> bike); M — TownFullMap открывается/закрывается на svitlodarsk/wurzburg, мини-карта присутствует;
    4 карты выбираются (Coastline/Costco/Svitlodarsk/Würzburg) через РадиоГруппу; настройки переживают
    ПЕРЕЗАПУСК приложения (porsche/drift/muted сохранились и применились — подзаголовок Porsche, SOUND OFF,
    DRIFT); RESTORE DEFAULTS сбрасывает (muted on, standard, coupe). Замечание: `__hud.style` отсутствует
    в хуке (только в React HUD) — не баг.

## ФАЗА «ГЕНЕРАЦИЯ КАРТ» — ДИЗАЙН (по запросу пользователя; запуск ПОСЛЕ 7 задач)
Согласованное:
  - Новый тип карты РЯДОМ с существующими (coast/costco/svitlodarsk/wurzburg).
  - Процедурная генерация по формулам/шуму (референс: Minecraft), внешне «проще».
  - Игрок задаёт пропорции ЛЕС / ПОЛЕ / ГОРОД: пресеты + «advanced settings» (вручную слайдеры) + сид.
  - ДВА режима:
    1) СОХРАНЯЕМАЯ карта — ограниченная площадь (порядка svitlodarsk/wurzburg), генератор строит
       TownData и пишет в /maps/<id>.json; дальше работает как обычная карта.
    2) БЕСКОНЕЧНАЯ (без сохранения) — рядом с игроком прорисовывается фикс. набор чанков (напр. 32);
       при отъезде мир в старых местах перегенерируется (не сохраняется), при возврате — регенерируется.
       Важно: в этой части никого нет — его «активно» не храним. Переключатель вынести в настройки И
       предупредить игрока, что бесконечный мир не сохраняется / регенерируется.
  - Технические риски для планирования: бесконечность создаёт чанковую генерацию вокруг игрока,
    что задевает createTown/физику/mini-map/makeHeight; между чанками нужны бесшовные стыки
    (единая makeHeight!), иначе авто «выезжает из текстур». Настоящее бесконечное с сохранением НЕ делаем.
  - Ключ безопасности: генератор должен выдавать ВАЛИДНЫЙ TownData (единая makeHeight для пола/дорог/
    физики; стык пол/дорога; без дыр), иначе регрессии как на Würzburg.
- Плановые задачи (после 7): G1 ген. ядро (TownData из шума+пропорций), G2 генератор/лоадер
     чанков+сохранение, G3 UI пресеты/advanced/сид/режим, G4 интеграция race.ts (регистрация типа,
     центр чанков вокруг игрока), G5 тестер/регрессия.
====================================================================

## G1 — app/gen-town.ts      статус: DONE (node-валидация tools/g1_validate.ts)
Проверено: gen-A default (295 зданий), gen-B city=.7 (621), gen-C size=3000 (299) — без выходов
за bounds, детерминизм OK, tsc --noEmit OK. Экспорты: generateTown, genElev, worldHeight.
  - Сетка дорог: mainSp=(cx1-cx0)/mN, круглое деление (дороги ложатся точно на bounds),
    +residential по центрам, сервисных нет (убраны как дубли).
  - Плотность блоков: cellW=floor(innerW/6), rand()>.5 пропуск.
  - kinds areas валидны (water flat -0.08; grass-растельные +0.015; pitch/parking/pedestrian +0.018).
  - spawn = nearRoad() по ближайшей дороге, yaw=atan2(dz,dx).

## G2 — app/gen-loader.ts      статус: DONE (node-валидация tools/g2_validate.ts)
Два режима:
  1) SAVED: generateSaved(o{seed,forest,field,city,name?}) -> {id,data} (id=slug(name)|'gen-<seed>');
     saveMap/saveGenerated пишет JSON в public/maps/<id>.json (+ mirror в dist/maps если существует);
     загрузка — существующая loadTownData(id) (app/town-data.ts, fetch /maps/<id>.json).
  2) INFINITE: getChunksAround(seed,cx,cz,radius=2) -> Map<'x,z',{x,z,data:TownData}> (ромб radius);
     generateChunk(seed,cx,cz) детерминирован по (seed,cx,cz); LRU-кэш 96 (unloadChunksBeyond).
Геометрия чанка: CHUNK=1200, SPACING=300 (кварталы), дороги = мировые линии k*300 (каждая 2-я w10
unclassified, остальные w6 residential), здания/рощи/пруд внутри чанка (кварталы не пересекают стыки).
Высота: глобальный шум worldHeight(x,z,seed) (амп 4-8, 5 октав) + genElev(res=8, чанк ровно 151×151).
  - ВАЖНО: makeHeight (app/town.ts) клиппит ix к nx-2 ПОСЛЕ расчёта tx=fx-ix, поэтому на
    границе x=X1 возвращает значение столбца X1-res, а НЕ X1. Сеам чиню в gen-loader: последний
    столбец nx-2 и последняя строка nz-2 элева, плюс угол, выставляются в worldHeight(P1) —
    обе смежные чанка дают одинаковую высоту на стыке (погрешность ~1e-8). tools/town.ts НЕ менялся.
Проверено tools/g2_validate.ts (esbuild+node): стыки x=0/1200 и z=1200 |ha-hb|<1e-8, дорога на
границе есть в обоих соседних чанках, реген одинаков (JSON-сравнение), getChunksAround(.,r=1)=5, спавн
на дороге (не в перекрёстке). Сохранённый режим: запись/чтение public/maps/<id>.json совпадает с данными.
Для G3 (UI) / G4 (race.ts интеграция): используемые точки — generateChunk/getChunksAround/makeWorldHeight.

## E-dock — Docker-деплой (статика → nginx)      статус: DONE (проверено: build + образ + HTTP)
Автор: агент E-dock (ветка «Эксплуатация», подветка docker; НЕ трогает page.tsx/globals.css).
Сделано (только новые файлы):
  - Dockerfile — multi-stage: node:22-alpine (npm install + npm run build) → dist/client → nginx:1.27-alpine.
    npm ci ЗАМЕНЁН на npm install: package-lock.json сгенерён на macOS, на linux/alpine npm ci падал
    (Missing @emnapi/runtime/@emnapi/core).
  - nginx.conf — статика, gzip; SPA-fallback try_files ... /index.html; кэш: /_next/static/* и /maps/*.json
    immutable 1y (+ Access-Control-Allow-Origin:* на /maps/), *.glb — 1y.
  - docker-compose.yml — build: ., порт 8080:80, restart: unless-stopped.
  - DOCKER.md — короткая инструкция (docker compose up -d --build → http://localhost:8080).
  - .dockerignore — clean context (node_modules/dist/.next/release/tools/.git/...).
Проверка (отчёт агента, перепроверено планировщиком частично):
  - npm run build OK — проект уже output:'export', dist/client (правка конфига не потребовалась).
  - GLB и /maps/*.json в dist/client — статика автоматически.
  - docker build PASS (~8.8s build stage); контейнер docker compose up поднят, HTTP-тесты:
    GET / → 200 (title AFTERGLOW), SPA deep route → 200, /maps/svitlodarsk.json и /maps/wurzburg.json → 200 + CORS *,
    /car_glb.glb (71MB) → 200, gzip: Content-Encoding: gzip, Cache-Control immutable 1y на _next/static и maps.
    После теста docker compose down — контейнер убран (подтверждено: docker ps пуст, образ оставлен).
Файлы: только новые (Dockerfile, nginx.conf, docker-compose.yml, Dockerfile не правился в E — новый).
Хвосты: нет. Вне скоупа: реальный деплой (DNS/CDN/HTTPS); порт менять в docker-compose.yml.
Следующему: E-mobile (адаптив телефонов) — ПОСЛЕ G3 (globals.css/page.tsx заняты G3).

## G3 — app/page.tsx + globals.css (UI генератора)      статус: DONE (cdp_gen_saved.mjs + cdp_gen_infinite.mjs)
## G4 — app/race.ts (интеграция генеративной карты)     статус: DONE (cdp_g4_gen_gameplay.mjs + cdp_g4_stream.mjs)
## Задача C — внутриигровая валюта $ + магазин машин (4 модели)   статус: DONE (cdp_wallet.mjs + cdp_shop.mjs)
Автор: Агент-C
Согласованный порядок: ШАГ 1 wallet-store.ts (изолирован) → ШАГ 2 конверсия очков→$ в race.ts
    (НЕ до завершения G5) → ШАГ 3 магазин/GARAGE/SHOP в page.tsx (после G5).
Файлы: app/wallet-store.ts (мой, готов), app/race.ts + app/page.tsx (ШАГи 2/3, ЖДУТ G5).
    НЕ трогаю чужие секции/файлы.
—— ШАГ 1 (DONE, проверено) ——
• Создан app/wallet-store.ts — изолированный store валюты/владения (по образцу settings-store.ts):
  ленивый кэш (cache===null → defaults/load), ключ 'afterglow.wallet', SSR-safe, защита от
  битого JSON. Тип Wallet {cash:number; cars:Record<WalletVehicle,boolean>},
  WalletVehicle='coupe'|'porsche'|'m8'|'bike' (локальный, без импорта race.ts — чтобы race
  мог импортировать этот модуль без цикла).
• API: getWallet(), addCash(n), spendCash(n):boolean, ownCar(id), buyCar(id,price):boolean
  (+списание, false при нехватке/уже owned), isOwned(id), resetWallet().
• PRICES: coupe 1500, porsche 6000, m8 9000, bike 3500 (bike в середине диапазона).
• Coupe owned по умолчанию; cash стартует 0. Экспоzed хук window.__wallet для CDP/тестов.
• Проверено: npx tsc --noEmit — ЧИСТ (EXIT 0, включая мой файл; глобальная ошибка G4 race.ts(118)
  к этому моменту уже закрыта). Юнит-тест tools/wallet_test.cjs (transpile esbuild + mock
  localStorage): дефолты/PRICES/добавление/списание/блок при нехватке/покупка/повторная
  покупка блокирована/ownCar/отрицательный ввод/дробь/персист после рестарта/коррапт JSON/
  resetWallet/__wallet — 17/17 PASS.
Статус обновлён: G5 → DONE, новая Задача Z (удаление ген-карт) держит page.tsx+globals.css.
ОБНОВЛЕНО 06.09:
- G5 по секции ниже: DONE (cdp_g5_static/saved + pixelcheck). race.ts СВОБОДЕН.
- Задача Z (удаление созданных игроком ген-карт) — DONE (commit a46ac3e, 36/36 PASS). page.tsx + globals.css СВОБОДНЫ — ШАГ 3 можно делать.
—— ШАГ 2 (DONE, применено в race.ts + CDP-проверено) ——
• Применено: import {addCash} из './wallet-store'; HUD-тип gets 'earned?:number'; стейт
  lastEarned/mapPausing; emit() ловит пульс earned и сбрасывает; bankScore() —
  n=ceil(score/100) (в пользу игрока), addCash(n), lastEarned=n, score=0,combo=0,emit();
  вызывается: вход в pause() (НЕ при открытии карты — mapPausing guard в setMapOpen),
  menu(), и вместо 'score=0' в selectMap() и selectGenWorld().
• __hud debug сейчас включает score/combo/earned (для CDP/тоста).
• Гейт: tsc — ЧИСТ, npm run build OK, electron-builder OK.
• tools/cdp_wallet.mjs (CDP end-to-end на fresh инстансе) — 0 FAILURES:
  coupe owned по умолчанию / 3 машины locked ✓; drift накапливает score ✓; map-open пауза НЕ
  банит (score сохраняется, cash не растёт) ✓; Esc-pause банк cash += ceil(score/100) (184.5→$2) ✓;
  пульс earned в __hud ✓; score после банка 0 ✓; персист localStorage ✓; menu() с 0 не дабл-банит ✓;
  cash переживает Page.reload ✓; консоль NONE ✓.
• Среда CDP (важно): rAF/физика живы ТОЛЬКО когда окно app реально видимо. В этой среде
  ready-to-show не срабатывает → окно не показывалось → физика мёртва. ПАТЧ electron/main.cjs:
  к once('ready-to-show') добавлен fallback setTimeout(show,2000) (mainWindow.isVisible() guard) —
  окно гарантированно показывается. Нормальным пользователям поведение не меняет (ready-to-show
  срабатывает раньше). Без патча CDP-гонки физики невозможны.
• Тест-приёмы (найдено в этой фазе): зомби-инстанс может держать порт 9222 (pkill -9 не всегда
  убивает все хелперы) — сверить ps/curl; при запуске конкурентного бинаря тест может подключиться
  к СТАРОМУ инстансу. Использовать Emulation.setIdleOverride+setFocusEmulationEnabled+
  Page.setWebLifecycleState(frozen→active) до и после PLAY; до накопления score держать W с
  поллом до 30с (физика «жива» рывками). Скриншоты/пиксельные проверки в фоне ненадёжны.
—— ШАГ 3 (DONE, применено в page.tsx + globals.css + CDP-проверено) ——
• page.tsx: GARAGE/SHOP — overlay 'CHOOSE CAR' — теперь 'GARAGE · SHOP': у owned машины SELECT/✓,
  у неowned цена + кнопка BUY (disabled+красная цена при нехватке, текст 'SHORT $N'/'BUY'); после
  покупки авто выбирается автоматически; баланс 'BALANCE · $ N' в шапке оверлея и '$ N' в header
  (.wallet-bal); тост '.coin-toast' «+$N DRIFTED» по пульсу HUD.earned (useEffect на hud.earned,
  timeout 2.6s); startVehicle при старте клампится на owned (startVeh = owned? : 'coupe') в
  startGame/createGenMap; travel-mode/меню используют startVeh. Импорт getWallet/buyCar/isOwned/PRICES.
• globals.css: добавлены .car-card.locked/.car-price(.poor)/.car-buy(.disabled)/.car-select/
  .wallet-bal/.coin-toast + @keyframes toast-in.
• Гейт: tsc ЧИСТ, npm run build OK, electron-builder OK.
• tools/cdp_shop.mjs (CDP end-to-end on fresh инстансе) — 0 FAILURES:
  BALANCE $0 в оверлее+header ✓; coupe selected/owned, прочие locked с ценой ($6,000/$3,500) ✓;
  BUY disabled+красная цена при нехватке ✓; addCash(6000)→BUY enabled, цена норма ✓; BUY porsche →
  cash 0, owned, выбран (✓), BUY исчез (повторная блокировка) ✓; bike locked при cash 0 ✓;
  персист localStorage ✓; PLAY → старт на купленном Porsche (__hud.vehicle=porsche, HUD PORSCHE) ✓;
  реальный дрифт → Esc → тост '+$1 DRIFTED' ✓; консоль NONE ✓.
Проверки UI магазина (покупка/списание/нехватка/старт) — валидны и повторяемы (запуск cdp_wallet.mjs
  и cdp_shop.mjs на fresh инстансе даёт стабильный результат).
Хвосты по Задаче C: закрыта. Осталось (по желанию пользователя): коммит feature-c-wallet+shop.
  Файлы под коммит: app/wallet-store.ts, app/race.ts, app/page.tsx, app/globals.css,
  electron/main.cjs (патч show-fallback — без него CDP-гонки невозможны в этой среде), NOTES.txt.
  tools/* (cdp_wallet/cdp_shop/wallet_test и диагностики) НЕ коммитятся.

## G5 — тестер/регрессия фазы генерации                  статус: DONE (cdp_g5_static/saved + pixelcheck)
Автор: один агент выполняет G3 → G4 → G5 ПОСЛЕДОВАТЕЛЬНО (без параллели внутри).
Цель G3: включить кнопку «Create your own» в page.tsx: оверлей Generate — пресеты
  (Default/Forest/Plains/City), Advanced (слайдеры Лес/Поле/Город + сид/random), переключатель
  режима «⚑ Save map» vs «∞ Infinite (no save)» с предупреждением. SAVED: saveGenerated(opts) →
  /maps/<id>.json, карта в реестре → loadTownData(id). INFINITE: регистрация особой карты
  (карточку/реестр правильно; Play забирает G4). НЕ трогать race.ts. Файлы: page.tsx, globals.css.
Цель G4: интеграция в race.ts — регистрация генеративной карты, центр чанков вокруг игрока
  (getChunksAround), async-догрузка без фризов, makeHeight для генеративной высоты.
Цель G5: тестер/регрессия: tsc/build/electron-builder/CDP на всех картах + saved- и infinite-режимах.
Проверка следующему: G3/G4/G5 по факту выполнения — tsc чист, CDP проходит, saved-карта играбельная,
  infinite-центр догружает чанки без дыр.

—— G3 (реализовано) ——
• page.tsx: карточка «CREATE YOUR OWN MAP» стала активной → экран 'gen' (оверлей Generator).
• Реестр генеративных карт: localStorage 'afterglow.genmaps', тип GenMap {id,name,mode:'saved'|'infinite',seed,forest,field,city}.
  Ген-карты рендерятся КАРТОЧКАМИ в списке 'CHOOSE MAP' (после статичных, номера 05+, тег GEN / INF),
  выбор через chooseMap(id), isGen() отделяет их от статичных (для них selectMap НЕ вызывается).
• Форма генератора: пресеты BALANCED/FOREST/PLAINS/CITY (PRESETS таблица; active-подсветка);
  Advanced сворачиваемый: 3 слайдера Лес/Поле/Город (0..1) + SEED (input number) + RANDOM;
  режим «SAVE MAP» vs «INFINITE NO SAVE» (предупреждение gen-warn для infinite; инпут имени только в saved);
  кнопка «GENERATE & PLAY» → createGenMap():
    - saved: generateSaved({seed,forest,field,city,name}) → id=slug(name) или 'gen-<seed>', data передаётся;
      void saveMap(id,data) (в браузере возвращает false, файл не пишется — это node-путь для /maps).
    - infinite: id='inf-<seed>'.
    - addGenMap() в реестр (persist), setMap(id)+localStorage 'afterglow.map', world-пейлоад в
      window.__genplay, вызов (game.current as GameApi)?.selectGenWorld?.(world) — НООП до G4,
      затем старт игры.
• Play (startGame) для ген-карт: регенерирует детерминированно из seed, шлёт тот же GenWorld в
  selectGenWorld (в текущей сборке race сам не дергается; подъём геймплея — задача G4).
• Генеративный мир в рендерере НЕ сохраняется на диск: node-запись недоступна
  (Electron sandbox/nodeIntegration:false, HTTP-сервер статический GET-only). К финалу:
  for saved — данные живут в памяти (GenWorld.data) + могут быть выгружены через saveGenerated в node-контексте;
  недостающее звено «запись из рендерера в /maps» — открыто для G4 (см. сoответствие G2).
• globals.css: стили .gen-* (пресеты grid, слайдеры с accent-color lime, seed, mode-кнопки,
  warn-плашка, кнопка генерации).
• CDP-проверки: tools/cdp_gen_saved.mjs (slug-id, пресеты, сид, save, персист после reload) и
  tools/cdp_gen_infinite.mjs (infinite-режим, карточка INF, Play) — оба зелёные на упакованном .app.
  tsc --noEmit чист, npm run build OK, electron-builder --mac --arm64 прошёл.
• Handoff для G4 (app/race.ts): реализовать selectGenWorld(w:GenWorld) — saved: привести town из
  w.data (или loadTownData(w.id) если файл есть); infinite: центр чанков вокруг игрока
  (getChunksAround(seed,cx,cz)), асинхронная догрузка/выгрузка без фризов, makeHeight==worldHeight,
  спавн на дороге, физика (4 колеса + lean), minimap/town-map заменить на gen-миникарту.

—— G4 (реализовано, app/race.ts) ——
• Импорты из gen-loader.ts: CHUNK, SPACING, generateChunk, chunkKey, worldHeight, loadGenMapData,
  type GenOptions; TownData via town-data. Добавлен корень genRoot=new T.Group() hidden.
• Стейт: genWorld{id?,mode:'saved'|'infinite',seed,params,ccx,ccz}, genTowns:Map<'x,z',{x,z,town}>,
  genSpawn{x,z,yaw}, genQueue[], genBuilding, genQueuedKeys:Set. selectGenWorld добавлен в api-объект
  (без него hook window.__race.selectGenWorld === undefined — известный симптом).
• selectGenWorld(w): genWorld=w, откат/облавить; saved → 1 чанк (0,0) town=createTown(data) из
  w.data/loadGenMapData(w.id) СИНХРОННО; infinite → ensureChunk(0,0) синхронно + ромб radius=2
  (13 чанков), остальные асинхронно очередью (drainGen в rAF, до N/frame по таймслайсу).
• updateGenArea (каждые 90 кадров, только infinite): chunkCenter → neededKeys (ромб radius=2),
  выезд за пределы → ccx/ccz=null (стоп догрузки); disposeChunkTown для не-нужных ключей
  (удаление group + dispose геометрий/материалов/textures), ensureChunk для новых (dedup по genQueuedKeys).
• genEntryAt(x,z) — город из genTowns по РЕАЛЬНЫМ bounds (для пола/физики/камеры);
  groundCollision (ген → town.collide или {hit:false}); groundY (ген → worldHeight(x,z,seed));
  updateCamera: для saved-gen кламп камеры в РЕАЛЬНЫЕ town.data.bounds (margin 48) — камера не
  выходит за карту; mm/probe работают по genRoot.children.
• reset()/loop(): спавн из genSpawn; в loop e.town.update(clock) для каждого чанка + updateGenArea;
  selectMap/dispose: teardownGen() (очистка стейта+меша) + applyWorldVisibility() (ген-мир скрыт,
  статичные видны). genActive()=genWorld!==null&&genRoot.visible.
• Движение/выход на foot работает на ген-террене (groundCollision/groundY-ветки для walking).
• Правка G3-файла page.tsx (минимум): addGenMap теперь ДЕДУПЛИЦИРУЕТ по id (был дубль карт в
  реестре при повторной генерации одного seed).
• Проверки (packaged .app, CDP 9222):
  - tools/cdp_g4_gen_gameplay.mjs: INFINITE 4242 — спавн (600,150), езда moved=79 speed=209,
    carY=1.38 (рельеф), replay (PLAY) сброс→(600,150) и езда; SAVED CITY 4242 — спавн (-1458,-1364)
    mm=-1.78 (внутри города), езда moved=112 speed=145, carY=-1.28; реестр: inf-4242 и
    generated-map-4242 по 1 экз (дедуп работает); СТАТИЧНАЯ coast после ген: едет, carY=0 — регрессия OK.
  - tools/cdp_g4_foot.mjs: E выход/ходьба/strafe на ген-мире работают; R→спавн ген (600,150).
  - tools/cdp_g4_stream.mjs: езда z:150→9772 (8 границ чанков): чанки ДОГРУЖАЮТСЯ на лету
    (probe на финальной позиции и +1500м дают хиты 139-141), carY следит за рельефом.
  - Регрессия G3 пройдена (cdp_gen_infinite + cdp_gen_saved — PASS на упакованном .app).
  - tsc --noEmit чист; npm run build OK; npx electron-builder --mac --arm64 OK.
• Нюанс для G5: `HANDLING.max` в м/с → HUD speed = v*3.6 (макс ~440 км/ч на прямой) — это норма.
  Спавн SAVED-карт (generateTown) аналитически в ЦЕНТРЕ карты (bounds симметричны [-sz,sz]),
  НЕ в [0,1200] — не баг, тест корректировался. Осталось отдельно проверить на G5:
  «выехать за границу saved» (камера клампится в bounds) и «полный цикл PLAY после restart».
  G3-файл page.tsx тронут МИНИМАЛЬНО (дедуп addGenMap) — для G5 регрессии статичных карт.

—— G5 (итоговая регрессия; сдача: код+сборка+report) ——
• Диагностика «не стартует PLAY / не едет» — ВСЁ артефакты CDP-обвязки, багов движка НЕТ:
  - Клик PLAY раньше готовности движка (ready=false → startGame return) = no-op. Движок boot
    (~0.5-1c после mount) ждать исх. стартExpr (bonus MAPS в .actions).
  - Отключённое/перекрытое окно Electron троттлит requestAnimationFrame: loop() race.ts драйвится
    rAF, физика только active&&!paused; visibilitychange-pause при document.hidden. Фикс-сет:
    Page.bringToFront + Emulation.setFocusEmulationEnabled(true) (rAF 60-120/1.2s вместо 1) и
    --disable-backgrounding-occluded-windows --disable-renderer-backgrounding
    --disable-background-timer-throttling при запуске .app; плюс
    Object.defineProperty(document,'hidden',{get:()=>false,configurable:true}) (выражение должно
    вернуть 1, иначе CDP «Object reference chain is too long»).
  - Radix .map-card = radio: синтетический .click() не даёт onValueChange; нужен настоящий
    Input.dispatchMouseEvent в центр карточки. ПОВТОРНЫЙ клик по УЖЕ выбранной карте = no-op
    (onValueChange только при изменении value) — поэтому «PLAY после restart» молча не происходил,
    а спавн-ассерты проходили по СТАРОМУ window.__hud. Фикс теста: сначала клик другой карты
    (Sunset Coast), затем ген-карты, каждый раз проверяя возврат .menu-bg.
  - until()/ev() ждут ВЫРАЖЕНИЕ-строку, не функцию — функция в Runtime.evaluate даёт
    CDP «Invalid parameters».
  - Сигнал старта игры: в .actions появляется кнопка MAPS (PLAY/драйв-бар живут стабильно).
• tools/cdp_g5_static.mjs — 16/16 PASS на packaged .app (зить: spawn, дорога, no-clip probe,
  езда/физ torque, air, спидометр, футаж, T/тюнинг, телепорт, 2-й город, pause, menu, restart,
  RESTART-live, settings, консоль чиста). Det check tools/g5_det_check.ts — generateSaved
  детерминирован, spawn (-1458,-1364), время ~72 мс (для saved, city=0.75).
• tools/cdp_g5_saved.mjs — 0 FAIL (полный цикл после fresh restart): реестр персист;
  engine-ready PLAY стартует ПЕРВЫЙ раз и спавнит saved-gen (-1458,-1364); карточки закрывают
  overlay; PLAY после restart реально рестартит (спавн -1418,-1274 ← выбранный saved); езда
  moves; ВЫЕЗД ЗА ГРАНИЦУ saved: car (-2126,-1251) за |x|>2000, mm finite, carY>=mm-1 (без
  sink/NaN), speed 434, управление работает (yawDelta 1.74, движение). Критерий «probe []» НЕВЕРНЫЙ:
  __probe всегда даёт >=1 (дорога/земля ген-мира достают дальше bounds) — использовать позицию
  за |x|>2000/|z|>2000. Консоль 0 ошибок/варнов.
  NOTE-тест: каждый cdp_g5_*.mjs — на СВЕЖЕМ .app (pkill+open). Подряд на одном инстансе: 
  последняя выбранная карта предыдущего сценария (wurzburg) остаётся запускной по умолчанию,
  «engine-ready PLAY spawns on saved gen» читает чужой спавн (напр. -2208,-1523) — контаминация
  состояния, не баг движка (на fresh -1458,-1364 ровно).
• Pixel check (tools/pixelcheck.py): экономики 0 дыр НЕТ — доказ. метрика skyLikeBottom60 на
  светлых open-field кадрах ФАЛС-позитив (солнце/дымка на горизонте: row-макс на y~61% высоты,
  цвет 243,242,233 ярче реального неба lum 173; в нижних 25% кадра sky-like 0.38%; текстуры
  stdev 15-23 — не плоско). Green 38-58% PASS. На бетонной карте sky 0.23% PASS / green 1.33%
  (утилитарный профиль). Документированное ограничение метрики: horizon-haze/glare триггерит
  порог <0.5% на open-field картах — это НЕ дыра рендера.
• Итог G5: tsc чисто, build OK, electron-builder --mac --arm64 OK (dmg+zip в release/).
  Код страницы НЕ менялся в этой фазе (правки только в tools/).

## Задача Z — удаление СОЗДАННЫХ игроком (генеративных) карт      статус: DONE (tools/cdp_delmap.mjs, 36/36 PASS)
Автор: агент Z
Файлы: ТОЛЬКО app/page.tsx + минимально app/globals.css (стили кнопки удаления .gen-del).
    НЕ трогать race.ts / gen-loader.ts / gen-town.ts / settings-store.ts / town-minimap.tsx.
Контекст: реестр ген-карт localStorage 'afterglow.genmaps' (loadGenRegistry/saveGenRegistry,
    тип GenMap), MAP_KEY 'afterglow.map', loadMap(); ген-карточки в оверлее CHOOSE MAP рендерятся
    отдельным map() по genMaps (тег GEN/INF), статичные maps (coast/costco/svitlodarsk/wurzburg) — НЕ трогаем.
—— Задача Z (реализовано) ——
• page.tsx: deleteGenMap(id,e): preventDefault+stopPropagation (клик по ✕ НЕ выбирает карту и НЕ
  закрывает оверлей), удаляет из genMaps + saveGenRegistry(); если удаляемая === выбранной map —
  setMap('coast') + запись MAP_KEY='coast' (без кругова на race: selectMap/selectGenWorld НЕ вызываются,
  движок в меню неактивен). PLAY при этом использует реестр (startGame: genMaps.find → miss → selectMap('coast')).
• page.tsx: в ген-карточках (map по genMaps) добавлена кнопка <button type="button" class="gen-del"
  aria-label="Delete <name>">✕</button>; сам лейбл получил класс 'gen-card'. Статичные карточки НЕ изменены —
  кнопка есть только у ген-карт.
• globals.css: .gen-card{position:relative}; .gen-card .map-tag{margin-right:36px} (обтекание, без
  наложения на ✕); .gen-del — абсолют, угол карточки 8×8, 28×28px, лаймовый крестик, hover-подсветка,
  cursor:pointer, active scale(.94), z-index:1. @media(max-width:600px): ✕ 34×34px (>28 touch-area E-mobile),
  топ-правый 6×6, tag margin 44px.
• ИСТОЧНИК ИСТИНЫ = реестр: deleteGenMap не трогает /maps/*.json (в браузере файлы и не пишутся);
  PLAY генерирует сохранённые карты детерминированно из seed из РЕЕСТРА (startGame) — удалённая карта
  после удаления не пересоздаётся (проверено CDP: PLAY после удаления стартует coast).
• CDP tools/cdp_delmap.mjs (packaged .app, CDP 9222, Page.bringToFront + focus):
  - PART1: изначально 4 статичные карточки без ✕; после генерации saved — 1 ген-карточка с ✕,
    статичные без; реальный pointer-клик по ✕ (Input.dispatchMouseEvent по центру, десктоп) удаляет
    карту, оверлей остаётся открытым (CHOOSE MAP), назад в меню.
  - PART2: удаление ВЫБРАННОЙ (infinite) карты → выбор сброшен на coast (карточка Coastline selected,
    MAP_KEY='coast'); reload → реестр пуст, coast персистится, ✕ нигде нет.
  - PART3: PLAY после удаления сохранённой карты → старт на coast (sub 'Selected map · Coastline
    circuit'), удалённая карта не пересоздана, MAP_KEY coast.
  - PART4: 375×667 (Emulation mobile) — ✕ в пределах viewport и угла карточки (не выходит за неё),
    34×34px, elementFromPoint в центре = сам ✕ (без наложения), клик → карточка удаляется, оверлей открыт.
  - Итог: 36 PASS / 0 FAIL. Примечание среды: под touch-CD: emulation Input.dispatchMouseEvent упирается
    в таймаут — фолбэк на JS-клик (классифицировано, hit-target доказан elementFromPoint).
• Проверка сборки: npx tsc --noEmit ЧИСТ (exit 0); npm run build OK; npx electron-builder --mac --arm64 OK
  (release/*.dmg+zip). .app с этими правками оставлен запущенным на 9222.
Проверить следующему: C (валюта/магазин) после G5 — page.tsx/globals.css свободны (мои правки коммичены);
  глобальная регрессия статичных карт не затронута (✕ только у .gen-card). Хвосты: 320px-крайний случай
  тем же тестом (✕ 34px в угол влезает); на реальных touch-устройствах Кейс pointer:coarse.
Коммиты: backup-z-pre (06250a0) → feature (см. git log).
====================================================================

## Задача F — визуальный «подвесочный» крен кузова + наклон камеры   статус: DONE (tools/cdp_f_suspension.mjs, 0 FAIL; регрессия cdp_g4_gen_gameplay + cdp_f_foot зелёные)
Автор: агент F
Файлы: ТОЛЬКО app/race.ts. НЕ трогать page.tsx/globals.css/wallet-store.ts/town-*/gen-*.
Цель: силовой крен поверх существующей эмпирической физики (массу/инерцию/точки контакта НЕ вводить).
    Читать только готовые сигналы физики (lateral/longitudinal/speed/throttle/brake/steer) — физику
    physics() НЕ менять; это чисто визуальный слой.
—— Задача F (реализовано, race.ts) ——
• Переменные: roll=0, pitch=0 (сглаживаемые крен кузова), переиспользуемые eYaw/eRoll/ePitch (Euler)
  и qRoll/qPitch (Quaternion) — ВСЕГО 0 аллокаций в цикле (память не растёт).
• car-ветка рендера кузова (в physics(), последняя else-ветка):
  - rollTarget=clamp(-lateral*min(speed,120)*0.0042, ±0.13) — знак «−»: при повороте налево (A)
    внешняя сторона (правый борт) уходит вниз; на прямой (lateral≈0) roll→0; max 0.13 рад (~7.5°).
  - pitchTarget=clamp(разгон: min(1,long*0.004)*0.055 − тормоз/назад: (SPACE|S при speed>6)*0.06, ±0.06);
    нос вниз при торможении, вверх при разгоне; dash-защита: при стоячке (speed≈0) pitch→0.
  - Сглаживание (инерция подвески): roll+=(rt-roll)*(1-exp(-dt*5)), pitch k=6.5.
  - Композиция: driven.quaternion = Euler(0,yaw,0) · RollZ(roll) · PitchX(pitch) — при yaw≠0 крен
    идёт вокруг ЛОКАЛЬНОЙ оси (при 90° — боком, не «шпагат»); кузов ровен на прямой (roll 0).
  - Байк-ветка (rotation.z=bikeLean) НЕ тронута; скор/stрік/yaw-физика, дрифт, score — нет.
• reset(): добавлен сброс roll=pitch=0 (ИСКЛЮЧИТЕЛЬНО визуальные переменные; логика спавна/физики
  reset не изменена) — после R/старта кузов сразу ровный, без остаточного крена.
• updateCamera(): для машин target camRoll=roll*0.3 со сглаживанием dt*6.5 (чуть агрессивнее кузова —
  мягко «догоняет», не раскачивает); для байка оставлено bikeLean*.5; для foot — 0. Кламп камеры в
  bounds (TOWN / saved-gen) и fov-отзыв от скорости СОХРАНЕНЫ.
• Дебаг-хук: window.__sus={roll,pitch,camRoll,yaw,mode} обновляется в emit() (только для CDP,
  React-HUD не затронут).
• Проверка сборки: npx tsc --noEmit ЧИСТ; npm run build OK; npx electron-builder --mac --arm64 OK.
• CDP tools/cdp_f_suspension.mjs (packaged .app, bringToFront+IdleOverride+focus, KeyW-гонка):
  coast: прямой roll≈0 (avg 0.000), speed range ~233 km/h (не упала); поворот A → roll −0.09..−0.13,
  D → +0.05..+0.13 (внешняя сторона, противоположные знаки); торможение SPACE → pitch −0.059 (нос вниз);
  камера camRoll плавно до ~0.039 (≈roll*0.3), возврат к ~0 без осцилляций; R сброс (speed→0),
  E exit/enter, M open/close, Esc pause — работают; Wurzburg: едет (345 km/h), roll/pitch в манёврах;
  консоль NONE. Итог основного теста: 0 FAILURES.
• Регрессия генеративных/статичных миров на свежих .app:
  - tools/cdp_g4_gen_gameplay.mjs — 0 FAIL: infinite (seed 4242) спавн/езда/рельеф/реплей, saved CITY
    спавн/езда/рельеф, реестр, coast-регрессия после ген — всё зелёное.
  - tools/cdp_f_foot.mjs (эквивалент cdp_g4_foot, который в tools/ был диагностическим без ассертов):
    gen-мир E exit → foot, ходьба (moved), E re-enter → car, R → спавн ген, roll≈0 после reset,
    консоль NONE — 0 FAILURES.
Проверить следующему: глобальный прогон D (трасса с обгоном) если в планах — race.ts HUD/API не менялись
    (только __sus добавлен); камера/физика ген-миров подтверждены; лимиты кена (0.13 рад) в пределах
    «не болтает» (по CDP-семплингу camRoll-шаги <0.04/250мс). Хвосты: (1) pitch при разгоне программно
    не проверялся отдельно (в логах PART1 виден pitch≈0.011 при разгоне — носовой подъём работает);
    (2) визуальный вкус (силуэт крена) — оценка требует foreground-окна; (3) на байке крен по-прежнему
    только bikeLean (как и было — вне скоупа F).
Коммиты: backup-f-1 (bc88f06) → feature-f-suspension.

====================================================================

## Задача CARS — два новых транспорта: BMW M4 GT3 (G82) + Ford Mustang GT3   статус: IN PROGRESS
Автор: Агент-CARS
Согласованный порядок: ШАГ A — ТОЛЬКО ассеты в public/ (изолированно, НЕ трогать
    race.ts/page.tsx/wallet-store.ts/settings-store.ts), коммит backup-cars-pre →
    feature-cars-assets; затем ШАГ B — интеграция в race.ts + page.tsx + wallet-store.ts,
    коммит backup-cars-b → feature-cars-vehicles.
Файлы ШАГ A: public/bmw_m4_gte.glb (из source/Untitled.glb), public/ford_mustang.glb
    (из ford_mustang_gt3__www.vecarz.com.glb); tools/ (служебные, не коммитятся).
Файлы ШАГ B: app/race.ts, app/settings-store.ts, app/wallet-store.ts, app/page.tsx.
    НЕ трогаю чужие секции/файлы.
Источники:
    /Users/bohdan/Desktop/cars/2022-full-carbon-bmw-m4-gt3-g82/source/Untitled.glb (46.6MB)
    /Users/bohdan/Desktop/cars/ford_mustang_gt3__www.vecarz.com.glb (70MB)
Both GLB self-contained (textures embedded).
—— ШАГ A (реализовано) ——
• M4 — ПРОБЛЕМА: Untitled.glb = ОДИН меш с 51 примитивом-материалом (единый узел
  bmw_m4_gt3_acc, rotation 90°X) → loadCarModelMulti не сможет выделить колёса
  (clusters!=4 → null → процедурный фолбэк). Решение: tools/bmw_split.mjs разбивает
  GLB на 51 узел/меш (по материалам, имена = material.name) и пишет public/bmw_m4_gte.glb.
• mustang — multi-mesh (85 мешей/261 узел) → копируется как есть в public/ford_mustang.glb.
• Проверка геометрии tools/cars_inspect.mjs (аналог wheelClusters по JSON+BIN):
  [впишут факт: оба по ровно 4 колеса FL/RL/FR/RR, перед/зад 2+2, tailMat].
• ИТОГ ШАГ A (реализовано, обе валидированы через loadCarModelMulti):
  • public/ford_mustang.glb (70,255,496 b, 84 меша, 260 узлов): из исходника удалён узел/меш
    Object_119 (mat_591, underbody-кронштейн s(0.43,0.62,0.65) c(-0.34,0.55,0.44)) — единственный
    ложный 5-й кластер (roundness-фильтр «близнеца» Object_110 не пропускает). Проверено
    tools/mustang_probe.mjs + tools/mustang_fix.mjs. Результат: ровно 4 кластера.
  • public/bmw_m4_gte.glb (60,931,288 b, 32 меша, 33 узла): Устарело bmw_split.mjs — автор-модель
    СТОИТ (length вдоль Y, высота вдоль Z): bake rotX(+90) «(x,y,z)→(x,−z,y)» в позиции в месте
    + новый identity-root (родительская rotation убрана). КРИТИЧНО: THREE boundingBox считается по
    ВСЕМУ POSITION accessor, а не по индексируемым вершинам → для каждого компонента пишутся
    СВОИ копии вершин (POSITION/NORMAL/TEXCOORD_0) + локальный index (u16/u32). Колёса — единые
    связные примитивы на все 4 колеса → бакетирование треугольников по 4 k-means якорям (R=0.80)
    в EXT_*_Wheel_0..3 (+ _stray); прочие примитивы — union-find components: крупные (≥2500
    треугольников) своими мешами, мелкие слиты в ОДИН meshes m4_interior_detail (position-only,
    широкий bbox → фильтр thin-X/round не пропускает). Сборщик tools/bmw_build.mjs.
  • cars_loadcheck (реальный лоадер): M4 → wheels FL(-0.89,-1.54,y0.35) RL(-0.86,1.34,y0.36)
    FR(0.88,-1.62,y0.36) RR(0.87,1.34,y0.36); mustang → FL(-0.90,-1.47,y0.34) RL(-0.87,1.31,y0.36)
    FR(0.86,-1.48,y0.34) RR(0.89,1.30,y0.39); обе: tailMat есть, textures 0, front z<0.
  • Важен нюанс: состояние в public/ НА МОМЕНТ МАСТЕР-СНИМКА EA78611 было older трактом
    (bmw_split), т.к. ШАГ B ещё не завершён; ПЛАНИРОВЩИК пускорил снимок — пере-проверить что в
    deployment фактически ушли финальные ассеты после feature-cars-assets.
  • tools/cars_loadcheck.mjs stripTextures приведён к версии cars_frame.mjs (рекурсивное удаление
    'index' внутри material extensions иначе GLTFLoader падает на extensions мёртвых текстур).
—— ШАГ B (реализовано) ——
• vehicle-union VehicleId: coupe|porsche|m8|m4|mustang|bike (app/race.ts L19 и пр.); в
  WalletVehicle/Settings.startVehicle те же 6 марок.
• race.ts: CAR_GLB += m4:'/bmw_m4_gte.glb', mustang:'/ford_mustang.glb'; CAR_NAMES +=
  'BMW M4 GT3','Ford Mustang GT3'; HANDLING += m4{accel:45,rev:50,max:141,...,grip:2.3,
  steer:1.45,latYaw:.22} (GT3-прото выше m8) и mustang{accel:46,rev:48,max:138,...,grip:1.35,
  steer:1.7,latYaw:.38} (muscle-drift: выше accel, НИЖЕ grip → больше слайд). Точки: цикл
  carGroups ['porsche','m8','m4','mustang'], garage {m4:null,mustang:null}, setLoad += m4/mustang,
  cast vid as 'porsche'|'m8'|'m4'|'mustang'. HANDLING существующих машин НЕ менялись.
• wallet-store.ts: PRICES{m4:12500,mustang:8500}, defaults() m4:false mustang:false.
• page.tsx: vehicles += m4 (Carbon GT3 · Most grip), mustang (V8 muscle · Loose tail · Big slides)
  перед bike — карточки SHOP: buy → owned + startVehicle, SHORT-$ при нехватке.
• GATE CDP tools/cdp_cars.mjs (паттерн cdp_wallet/cdp_shop, port 9222): reset→reload→menu.
  Проверено 36/36 OK, exit 0: карточки m4 $12,500 / mustang $8,500, locked, SHORT при $0/$3000,
  покупка обеих (cash→0, persisted), PLAY на купленном m4 (vehicle id m4, travel-mode M4 GT3),
  GLB m4+mustang загружены в garage (__load), W-гонка m4 (drift score рост), __sus крен
  (roll/pitch/yaw реагируют), mustang drive, регрессия coupe/porsche/m8/bike (все заводятся/едут).
  Консоль: только среда-предупреждения Electron CSP + React#418 hydration self-heal (из-за
  setStartVehicle≠coupe при reload — PRE-EXISTING, не CARS-регрессия), GLTFLoader min/max — шум.
• Сборка/пакетирование: tsc --noEmit 0; npm run build 0 (dist/client с новым bmw_m4_gte.glb +
  ford_mustang.glb); electron-builder mac arm64 (DMG+ZIP собраны; подпись skipped как обычно).
• Итоговые git: c8b04ee feature-cars-assets (bmw_m4_gte.glb 61М + NOTES ШАГ A), далее
  feature-cars-vehicles (app/* 4 файла + NOTES ШАГ B). tools/ и cdp_cars.mjs НЕ коммитятся.

====================================================================

## Задача V1 — публикация игры в интернет через Vercel (GitHub-import, Hobby)   статус: IN PROGRESS
Автор: агент V1 (промпт от планировщика).
Решение пользователя (подтверждено): Вариант 1 — Vercel через GitHub-import; репо ПУБЛИЧНЫЙ;
    домен бесплатный https://<proj>.vercel.app; аккаунты GitHub и Vercel у пользователя ЕСТЬ.
Доступ к среде: у пользователя открыт Google Chrome, залогинен в GitHub и Vercel; агенту дано
    право работать с этим браузером через computer-use «CLI-Anything» (скриншоты/клики/ввод/фокус).
    git push — с машины (MAC, локальный git в game/).
Файлы: ТОЛЬКО инфраструктура — .gitignore, vercel.json, .vercelignore (и при необходимости
    next.config только по мотиву деплоя). ЗАПРЕЩЕНО трогать: app/** (race.ts, page.tsx, town-*,
    gen-*, settings-store.ts, wallet-store.ts, visuals.ts, actors.ts, town-minimap.tsx),
    public/maps/*.json, .glb-файлы, electron/, package.json/package-lock.json (без отдельной задачи).
Факты, проверенные планировщиком:
    • Сборка НЕстандартная: next НЕТ в package.json; сборщик vinext 1.0.0-beta.5; output:'export'.
      npm run build → dist/client (203М статика) + dist/server/wrangler.json (задел Cloudflare — для
      Vercel НЕ нужен). Локально node v24.12/npm 11.6.2, engines >=22.13.
    • public/ до CARS: ~187М GLB (car_glb 68М, bmw_s1000rr 45М, porshe 26М, m8 20М, harley 15М,
      super_human 9.6М). CARS ШАГ A добавляет bmw_m4_gte.glb (~46М) + ford_mustang.glb (~70М).
      Все файлы <100МБ → GitHub примет. Сумма в репо может быть ~300-400М (GitHub устаёт на ~1ГБ — ок).
    • Vercel Hobby: CLI-загрузка исходников ограничена 100МБ → CLI-путь НЕ используем, только
      Git-import. Лимит файлов 15000, билд 45мин, диск 32ГБ.
    • git: remote НЕТ; дерево локально чистое (изменён только tsconfig.tsbuildinfo + untracked tools/).
      gh и vercel CLI в PATH НЕ установлены (можно поставить vercel CLI, но основной путь — Git-import).
Контракт V1 (acceptance):
  1) backup-коммит «backup-v1-pre» до работы, feature-коммит после.
  2) .gitignore дополнен: /release/, /dist/, /tools/, /.vercel/, tsconfig.tsbuildinfo, *.dmg,
     *.zip, builder-debug.yml, zir3jUqF/ (+ проверить отсутствие секретов: socket keys/TLS/.env).
     git status: node_modules/, release/, dist/, tools/ и tsbuildinfo в push НЕ попадают.
  3) Публичный GitHub-репо (имя: спросить у пользователя или по-умолчанию afterglow-drift), remote
     origin добавлен, текущий main запушен (снимок: CARS-ассеты если уже в public/ — войдут).
  4) Vercel: New Project → Import из GitHub. Настройки: Framework Preset Other (не Next.js),
     Build Command «npm run build», Install «npm install», Output Directory «dist/client»,
     Node.js 22.x. Деплой зелёный.
  5) Прод-проверка (curl + Chrome/CLI-Anything):
     / 200; /maps/svitlodarsk.json и /maps/wurzburg.json 200; /car_glb.glb 200 + ожидаемый размер;
     меню→кост→PLAY стартует, KeyW едет, HUD speed>0, minimap жив; консоль без ошибок;
     mobile 375×667 — меню/карточки влезают, PLAY работает.
  6) Если Vercel не отдаёт большой GLB (>~50М): КОД НЕ МЕНЯТЬ — пометить BLOCKED по этому пункту,
     в NOTES хвост: «вынести большие GLB на Vercel Blob (Hobby 1ГБ) + замена URL» (отдельная задача).
     Vercel Pro/оплату Не включать без согласования с пользователем.
  7) NOTES-отчёт: URL, headers-факты, что DONE/что BLOCK, какие файлы «заняты» (НЕ app/ — они свободны,
     но CARS держит race.ts/page.tsx/wallet-store.ts), хвосты. Коммит результата «feature-v1-vercel».
Координация: V1 и CARS ПАРАЛЛЕЛЬНЫ (инфраструктурные файлы vs app/). V1 НЕ трогает файлы CARS.
    После V1: автодеплой Vercel будет на каждый push main — все фиче-коммиты уйдут в прод сами.

